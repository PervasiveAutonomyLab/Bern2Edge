# Bern2Edge Python package

This directory contains the reusable Python source imported as `bern2edge`.
It provides models, data loaders, knowledge-distillation utilities, Bernstein
activations, and rule extraction.

Experiment drivers, checkpoints, and paper results are kept in the dataset and
table directories at the repository root.
