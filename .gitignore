__pycache__/
*.py[cod]
*$py.class
.venv/

# Packaging / editable-install artifacts
*.egg-info/
build/
dist/

# Editor / assistant state
.claude/

# Bern2Edge.pdf is intentionally tracked: the CODES 2026 artifact instructions
# require the accepted paper to be included with the artifact.

# ACS — the folktables PUMS cache (~523 MB, re-downloaded on demand; see REQUIREMENTS.md)
ACS/data/

# MAGIC — regenerable outputs (shipped 5_fold_results.csv / results/table_x.csv are authoritative)
MAGIC/candidates_cache/
MAGIC/extraction_results.csv

# Transformer -- from-scratch training output (the shipped models/ are authoritative)
Transformer/scratch/
Transformer/results/*.log
