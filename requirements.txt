# Bern2Edge — Python dependencies
# Tested with Python 3.10. Versions are lower bounds known to work; newer
# patch releases should be fine.
numpy>=1.24
torch>=2.0
scikit-learn>=1.2
pandas>=1.5
scipy>=1.10
matplotlib>=3.6
openml>=0.13        # only for fetching the Adult dataset from OpenML
folktables>=0.0.12  # only for the ACS Income distribution-shift experiment (ACS/)
transformers>=4.30  # only for the TinyBERT4 FFN-substitution experiment (Transformer/)
datasets>=2.14      # only for the TinyBERT4 FFN-substitution experiment (SST-2 loading)

# auto_LiRPA — ONLY for the MAGIC ReLU certified column (TABLE X, regenerated from scratch).
# NOT installable via this file: the PyPI release pins torch<1.13 and would downgrade torch.
# Install from GitHub instead (see INSTALL.md), it does not touch your torch:
#   pip install --no-deps git+https://github.com/Verified-Intelligence/auto_LiRPA.git
#   pip install graphviz
# Reproducing the shipped MAGIC tables (make_table5.py / make_table_x.py) does NOT need it.
